<?xml version="1.0" encoding="utf-8" ?>
<Application
    x:Class="FluentSensors.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:fh="using:FluentSensors">

    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
                <!--  other merged dictionaries here  -->
            </ResourceDictionary.MergedDictionaries>

            <ResourceDictionary.ThemeDictionaries>

                <!--  === dark mode (default theme dictionary) ===  -->

                <!--  base taskbar background: #202020 (32)  -->
                <ResourceDictionary x:Key="Default">
                    <!--  1. taskbar button normal hover & pressed backgrounds (alpha white on #202020)  -->
                    <!--  hover -> target #2D2D2D (45): alpha 15 (0x0F) -> #0FFFFFFF  -->
                    <SolidColorBrush x:Key="TaskbarButtonHoverBackground" Color="#0FFFFFFF" />
                    <!--  pressed -> target #2A2A2A (42): alpha 11 (0x0B) -> #0BFFFFFF  -->
                    <SolidColorBrush x:Key="TaskbarButtonPressedBackground" Color="#0BFFFFFF" />

                    <!--  2. taskbar button borders (dark mode)  -->
                    <!--
                        the border is never transparent: it carries the #1DFFFFFF top highlight and the same fill
                        brush on the sides & bottom, so it always paints its 1px (no size shift between states) but
                        reads as invisible where it should; #0FFFFFFF over #202020 == the hover fill
                        top stop is #1D (not #16): the stroke composites over the bare bar now that the fill is
                        inset, so it needs the higher alpha to land ~#404040 and match the native taskbar highlight
                    -->
                    <LinearGradientBrush x:Key="TaskbarButtonBorderBrush" StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Offset="0.0" Color="#1DFFFFFF" />
                        <GradientStop Offset="0.10" Color="#0FFFFFFF" />
                        <GradientStop Offset="1.0" Color="#0FFFFFFF" />
                    </LinearGradientBrush>
                    <!--
                        normal pressed border: sole source of the pressed border (StrokeBorder is off in this state),
                        so the top stop is self-sufficient at #1FFFFFFF -> #3B3B3B; sides & bottom #10FFFFFF -> #2E2E2E
                    -->
                    <LinearGradientBrush x:Key="TaskbarButtonPressedBorderBrush" StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Offset="0.0" Color="#1FFFFFFF" />
                        <GradientStop Offset="0.12" Color="#10FFFFFF" />
                        <GradientStop Offset="1.0" Color="#10FFFFFF" />
                    </LinearGradientBrush>

                    <!--  3. active flyout state (when flyout is currently open)  -->
                    <!--  active hover -> target #323232 (50): alpha 21 (0x15) -> #15FFFFFF  -->
                    <SolidColorBrush x:Key="TaskbarButtonActiveHoverBackground" Color="#15FFFFFF" />
                    <!--
                        active hover border: same #1DFFFFFF top highlight as normal hover (~#404040, native match),
                        active-hover fill brush on sides & bottom so the 1px there reads as invisible
                    -->
                    <LinearGradientBrush x:Key="TaskbarButtonActiveHoverBorderBrush" StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Offset="0.0" Color="#1DFFFFFF" />
                        <GradientStop Offset="0.10" Color="#15FFFFFF" />
                        <GradientStop Offset="1.0" Color="#15FFFFFF" />
                    </LinearGradientBrush>
                    <!--  active pressed -> target #2A2A2A (42): alpha 11 (0x0B) -> #0BFFFFFF  -->
                    <SolidColorBrush x:Key="TaskbarButtonActivePressedBackground" Color="#0BFFFFFF" />
                    <!--  no visible border in this state, so it takes the fill brush (never Transparent) and paints its 1px  -->
                    <SolidColorBrush x:Key="TaskbarButtonActivePressedBorderBrush" Color="#0BFFFFFF" />

                    <!--  4. flyout window styling (dark mode)  -->
                    <!--
                        the dark literals sit a few levels below the tone they actually show; they were dialed in
                        by comparing screenshots with the native Windows surfaces, where dark values read back
                        lighter than they were written
                        that offset belongs to the display it was tuned on; another monitor measures its own
                        numbers, so the second value on each line is what was seen here and not a spec
                    -->
                    <!--  content area -> literal #242424, reads as #292929 here, opaque  -->
                    <!--  (the bottom bar paints its own color, see 6)  -->
                    <SolidColorBrush x:Key="FlyoutWindowBackground" Color="#242424" />
                    <!--  window border -> literal #464646, reads as #484848 here  -->
                    <SolidColorBrush x:Key="FlyoutWindowBorderBrush" Color="#464646" />

                    <!--  5. flyout graphs overlay (dark mode)  -->
                    <!--  only used in the blur modes, where it lifts the graphs area off the glass  -->
                    <!--  (the opaque mode takes its content color from FlyoutWindowBackground and uses no overlay)  -->
                    <SolidColorBrush x:Key="FlyoutGraphsBackground" Color="#0AFFFFFF" />

                    <!--  6. flyout bottom bar (dark mode)  -->
                    <!--  bottom bar -> literal #1C1C1C, reads as #222222 here  -->
                    <!--  opaque and independent of the content area  -->
                    <SolidColorBrush x:Key="FlyoutBottomBarBackground" Color="#1C1C1C" />

                    <!--  7. flyout bottom bar separator (dark mode)  -->
                    <!--  1px stroke between content area and bottom bar  -->
                    <!--  literal #181818, reads as #1E1E1E here  -->
                    <SolidColorBrush x:Key="FlyoutBottomBarSeparatorBrush" Color="#181818" />
                    <!--
                        the same stroke in the blur modes, where the native line is translucent and darkens the
                        material instead of covering it, so it keeps letting the backdrop through
                    -->
                    <SolidColorBrush x:Key="FlyoutBottomBarSeparatorOnGlassBrush" Color="#1F000000" />
                </ResourceDictionary>


                <!--  === light mode theme dictionary ===  -->

                <!--  base taskbar background: #EDEDED (237)  -->
                <ResourceDictionary x:Key="Light">
                    <!--  1. taskbar button normal hover & pressed backgrounds (alpha white on #EDEDED)  -->
                    <!--  hover -> target #F7F7F7 (247): alpha 142 (0x8E) -> #8EFFFFFF  -->
                    <SolidColorBrush x:Key="TaskbarButtonHoverBackground" Color="#8EFFFFFF" />
                    <!--  pressed -> target #F3F3F3 (243): alpha 85 (0x55) -> #55FFFFFF  -->
                    <SolidColorBrush x:Key="TaskbarButtonPressedBackground" Color="#55FFFFFF" />

                    <!--  2. taskbar button border (normal hover & pressed)  -->
                    <!--
                        alpha black so it tracks the bar; calibrated against the transparency-on light taskbar (~#F0F0F0)
                        to land top & sides #E7E7E7, bottom #DDDDDD there; over the #EDEDED solid bar it reads ~2 levels darker
                    -->
                    <LinearGradientBrush x:Key="TaskbarButtonBorderBrush" StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Offset="0.0" Color="#0A000000" />
                        <GradientStop Offset="0.75" Color="#0A000000" />
                        <GradientStop Offset="1.0" Color="#14000000" />
                    </LinearGradientBrush>
                    <!--
                        normal pressed border: sole source of the pressed border now (StrokeBorder is off in this state),
                        same alpha-black values as TaskbarButtonBorderBrush -> top & sides #E7E7E7, bottom #DDDDDD
                    -->
                    <LinearGradientBrush x:Key="TaskbarButtonPressedBorderBrush" StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Offset="0.0" Color="#0A000000" />
                        <GradientStop Offset="0.75" Color="#0A000000" />
                        <GradientStop Offset="1.0" Color="#14000000" />
                    </LinearGradientBrush>

                    <!--  3. active flyout state (when flyout is currently open)  -->
                    <!--  active hover -> target #FAFAFA (250): alpha 184 (0xB8) -> #B8FFFFFF  -->
                    <SolidColorBrush x:Key="TaskbarButtonActiveHoverBackground" Color="#B8FFFFFF" />
                    <!--
                        active hover border: alpha black, calibrated against the transparency-on light taskbar (~#F0F0F0)
                        to land top & sides #E5E5E5, bottom #D4D4D4 there
                    -->
                    <LinearGradientBrush x:Key="TaskbarButtonActiveHoverBorderBrush" StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Offset="0.0" Color="#0C000000" />
                        <GradientStop Offset="0.75" Color="#0C000000" />
                        <GradientStop Offset="1.0" Color="#1E000000" />
                    </LinearGradientBrush>

                    <!--  active pressed -> target #F3F3F3 (243): alpha 85 (0x55) -> #55FFFFFF  -->
                    <SolidColorBrush x:Key="TaskbarButtonActivePressedBackground" Color="#55FFFFFF" />
                    <!--
                        active pressed border: alpha black over the bare bar (the fill layer is inset like the others now),
                        calibrated against the transparency-on light taskbar (~#F0F0F0) to land #E7E7E7 on all 4 sides
                    -->
                    <SolidColorBrush x:Key="TaskbarButtonActivePressedBorderBrush" Color="#0A000000" />

                    <!--  4. flyout window styling (light mode)  -->
                    <!--
                        light barely needs the correction described in the dark section, the offset stays under
                        one level above roughly 230
                    -->
                    <!--  content area -> #F2F2F2, opaque; the bottom bar paints its own color, see 6  -->
                    <SolidColorBrush x:Key="FlyoutWindowBackground" Color="#F2F2F2" />
                    <!--  window border -> literal #C0C0C0, reads as #BFBFBF here  -->
                    <SolidColorBrush x:Key="FlyoutWindowBorderBrush" Color="#C0C0C0" />

                    <!--  5. flyout graphs overlay (light mode)  -->
                    <!--  only used in the blur modes, where it lifts the graphs area off the glass  -->
                    <!--  (the opaque mode takes its content color from FlyoutWindowBackground and uses no overlay)  -->
                    <SolidColorBrush x:Key="FlyoutGraphsBackground" Color="#38FFFFFF" />

                    <!--  6. flyout bottom bar (light mode)  -->
                    <!--  bottom bar -> target #EDEDED (237), opaque and independent of the content area  -->
                    <SolidColorBrush x:Key="FlyoutBottomBarBackground" Color="#EDEDED" />

                    <!--  7. flyout bottom bar separator (light mode)  -->
                    <!--  1px stroke between content area and bottom bar  -->
                    <!--  literal #DEDEDE, reads as #DDDDDD here  -->
                    <SolidColorBrush x:Key="FlyoutBottomBarSeparatorBrush" Color="#DEDEDE" />
                    <!--
                        the same stroke in the blur modes, where the native line is translucent and darkens the
                        material instead of covering it, so it keeps letting the backdrop through
                    -->
                    <SolidColorBrush x:Key="FlyoutBottomBarSeparatorOnGlassBrush" Color="#13000000" />
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>


            <!--  === custom subtle button ===  -->
            <!--
                the platform SubtleButtonStyle fades its fill in over 83 ms but swaps the border brush in the
                frame the state changes, and both resolve to the same brush; a hover therefore starts as a 1px
                outline in the final color around a still empty button
                every state brush below is the one the platform style uses, the border just stays transparent in
                all of them, and the fill is sized to the outer border edge so it still covers the full box
            -->
            <Style x:Key="SubtleBarButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="{ThemeResource SubtleButtonBackground}" />
                <Setter Property="BackgroundSizing" Value="OuterBorderEdge" />
                <Setter Property="Foreground" Value="{ThemeResource SubtleButtonForeground}" />
                <Setter Property="BorderBrush" Value="{ThemeResource SubtleButtonBorderBrush}" />
                <Setter Property="BorderThickness" Value="{ThemeResource ButtonBorderThemeThickness}" />
                <Setter Property="Padding" Value="{StaticResource ButtonPadding}" />
                <Setter Property="HorizontalAlignment" Value="Left" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="FontFamily" Value="{ThemeResource ContentControlThemeFontFamily}" />
                <Setter Property="FontWeight" Value="Normal" />
                <Setter Property="FontSize" Value="{ThemeResource ControlContentThemeFontSize}" />
                <Setter Property="UseSystemFocusVisuals" Value="{StaticResource UseSystemFocusVisuals}" />
                <Setter Property="FocusVisualMargin" Value="-3" />
                <Setter Property="CornerRadius" Value="{ThemeResource ControlCornerRadius}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <ContentPresenter
                                x:Name="ContentPresenter"
                                Padding="{TemplateBinding Padding}"
                                HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                AutomationProperties.AccessibilityView="Raw"
                                Background="{TemplateBinding Background}"
                                BackgroundSizing="{TemplateBinding BackgroundSizing}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Content="{TemplateBinding Content}"
                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                ContentTransitions="{TemplateBinding ContentTransitions}"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                Foreground="{TemplateBinding Foreground}">
                                <!--  the value of ControlFasterAnimationDuration  -->
                                <!--  (spelled out because BrushTransition.Duration is a TimeSpan and the platform keys are strings; the platform button hardcodes it the same way)  -->
                                <ContentPresenter.BackgroundTransition>
                                    <BrushTransition Duration="0:0:0.083" />
                                </ContentPresenter.BackgroundTransition>

                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />

                                        <VisualState x:Name="PointerOver">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Background">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SubtleButtonBackgroundPointerOver}" />
                                                </ObjectAnimationUsingKeyFrames>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Foreground">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SubtleButtonForegroundPointerOver}" />
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>

                                        <VisualState x:Name="Pressed">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Background">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SubtleButtonBackgroundPressed}" />
                                                </ObjectAnimationUsingKeyFrames>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Foreground">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SubtleButtonForegroundPressed}" />
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>

                                        <VisualState x:Name="Disabled">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Background">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SubtleButtonBackgroundDisabled}" />
                                                </ObjectAnimationUsingKeyFrames>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Foreground">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SubtleButtonForegroundDisabled}" />
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                            </ContentPresenter>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>


            <!--  === subtle tile button ===  -->
            <!--
                the same look as the subtle bar button above, plus the whole content fading while the button is
                held: a tile carrying an image or a colored badge has nothing that follows Foreground, so the
                platform press state reaches its text and leaves everything beside it at full strength
                a Style cannot add a VisualState through BasedOn, which is why this carries its own template
            -->

            <!--  how far the whole content fades in each state, Foreground is left alone so everything fades alike  -->
            <x:Double x:Key="TileButtonContentOpacityHover">0.95</x:Double>
            <x:Double x:Key="TileButtonContentOpacityPressed">0.80</x:Double>
            <x:Double x:Key="TileButtonContentOpacityDisabled">0.45</x:Double>

            <Style x:Key="SubtleTileButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="{ThemeResource SubtleButtonBackground}" />
                <Setter Property="BackgroundSizing" Value="OuterBorderEdge" />
                <Setter Property="Foreground" Value="{ThemeResource SubtleButtonForeground}" />
                <Setter Property="BorderBrush" Value="{ThemeResource SubtleButtonBorderBrush}" />
                <Setter Property="BorderThickness" Value="{ThemeResource ButtonBorderThemeThickness}" />
                <Setter Property="Padding" Value="{StaticResource ButtonPadding}" />
                <Setter Property="HorizontalContentAlignment" Value="Center" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="FontFamily" Value="{ThemeResource ContentControlThemeFontFamily}" />
                <Setter Property="FontWeight" Value="Normal" />
                <Setter Property="FontSize" Value="{ThemeResource ControlContentThemeFontSize}" />
                <Setter Property="UseSystemFocusVisuals" Value="{StaticResource UseSystemFocusVisuals}" />
                <Setter Property="FocusVisualMargin" Value="-3" />
                <Setter Property="CornerRadius" Value="{ThemeResource ControlCornerRadius}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <ContentPresenter
                                x:Name="ContentPresenter"
                                Padding="{TemplateBinding Padding}"
                                HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                AutomationProperties.AccessibilityView="Raw"
                                Background="{TemplateBinding Background}"
                                BackgroundSizing="{TemplateBinding BackgroundSizing}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Content="{TemplateBinding Content}"
                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                ContentTransitions="{TemplateBinding ContentTransitions}"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                Foreground="{TemplateBinding Foreground}">
                                <!--  the same 83 ms fill fade the bar button above spells out  -->
                                <ContentPresenter.BackgroundTransition>
                                    <BrushTransition Duration="0:0:0.083" />
                                </ContentPresenter.BackgroundTransition>

                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />

                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="ContentPresenter.Background" Value="{ThemeResource SubtleButtonBackgroundPointerOver}" />
                                                <Setter Target="ContentPresenter.Opacity" Value="{StaticResource TileButtonContentOpacityHover}" />
                                            </VisualState.Setters>
                                        </VisualState>

                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Target="ContentPresenter.Background" Value="{ThemeResource SubtleButtonBackgroundPressed}" />
                                                <Setter Target="ContentPresenter.Opacity" Value="{StaticResource TileButtonContentOpacityPressed}" />
                                            </VisualState.Setters>
                                        </VisualState>

                                        <!--  (the same fade as the two above, only further down)  -->
                                        <VisualState x:Name="Disabled">
                                            <VisualState.Setters>
                                                <Setter Target="ContentPresenter.Background" Value="{ThemeResource SubtleButtonBackgroundDisabled}" />
                                                <Setter Target="ContentPresenter.Opacity" Value="{StaticResource TileButtonContentOpacityDisabled}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                            </ContentPresenter>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>


            <!--  === source link button ===  -->
            <!--
                the link look of the info popup, shared so the release dialog can carry the same one
                the negative left margin is what lets the padded hit area sit flush with the text above it
            -->
            <Style x:Key="SourceLinkButtonStyle" TargetType="HyperlinkButton">
                <Setter Property="Padding" Value="6,2,6,2" />
                <Setter Property="Margin" Value="-6,0,0,0" />
                <Setter Property="CornerRadius" Value="4" />
                <Setter Property="HorizontalAlignment" Value="Left" />
                <Setter Property="HorizontalContentAlignment" Value="Left" />
                <Setter Property="FontSize" Value="12" />
            </Style>

            <!--  === page link button ===  -->
            <!--
                the same link look one size up, for links that sit on a page rather than inside a popup
                the negative margin of the style above is dropped on purpose: in a row it pulls every link
                towards its neighbour, not just the first one against the left edge
            -->
            <Style
                x:Key="PageLinkButtonStyle"
                BasedOn="{StaticResource SourceLinkButtonStyle}"
                TargetType="HyperlinkButton">
                <Setter Property="Margin" Value="0" />
                <Setter Property="FontSize" Value="14" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
