<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="FluentSensors.Features.Start.ReleaseNotesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!--  (no padding on a scroller, it clips what it is meant to scroll; the children carry the inset)  -->
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="28,4,28,28" Spacing="16">

            <!--  header image  -->
            <!--  (pulled out of the markdown by the renderer, and kept on disk by ReleaseCatalog for offline)  -->
            <Border
                x:Name="HeroBorder"
                CornerRadius="{ThemeResource OverlayCornerRadius}"
                Visibility="Collapsed">
                <Image
                    x:Name="HeroImage"
                    MaxHeight="230"
                    HorizontalAlignment="Stretch"
                    Stretch="UniformToFill" />
            </Border>

            <StackPanel Spacing="4">
                <TextBlock x:Name="TitleText" Style="{ThemeResource SubtitleTextBlockStyle}" />
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <TextBlock
                        x:Name="DateText"
                        VerticalAlignment="Center"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Style="{ThemeResource CaptionTextBlockStyle}" />
                    <HyperlinkButton
                        x:Name="GitHubLink"
                        Padding="0"
                        Content="View on GitHub" />
                </StackPanel>
            </StackPanel>

            <!--  release notes  -->
            <!--  (a panel rather than a RichTextBlock, so callouts can be real Borders)  -->
            <StackPanel x:Name="NotesHost" />

            <TextBlock
                x:Name="EmptyText"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{ThemeResource BodyTextBlockStyle}"
                Text="This release was published without notes"
                TextWrapping="Wrap"
                Visibility="Collapsed" />
        </StackPanel>
    </ScrollViewer>
</Page>
