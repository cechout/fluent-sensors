<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="FluentSensors.Features.Start.ReleaseNotesControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Width="400" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  heading  -->
        <StackPanel Grid.Row="0" Spacing="1">
            <TextBlock
                x:Name="HeadingText"
                Style="{ThemeResource BodyStrongTextBlockStyle}"
                Text="What's new" />
            <TextBlock
                x:Name="SubheadingText"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{ThemeResource CaptionTextBlockStyle}" />
        </StackPanel>

        <!--  notes  -->
        <!--
            the scrolling lives here rather than on the flyout presenter, which has its own scroll switched off;
            nesting the two would leave the wheel hitting whichever one grabbed it first
        -->
        <ScrollViewer
            x:Name="NotesScroller"
            Grid.Row="1"
            MaxHeight="420"
            HorizontalScrollMode="Disabled"
            VerticalScrollBarVisibility="Auto">
            <RichTextBlock
                x:Name="NotesBlock"
                IsTextSelectionEnabled="True"
                TextWrapping="Wrap" />
        </ScrollViewer>

        <!--  the two states that replace the notes while there are none to show  -->
        <ProgressRing
            x:Name="LoadingRing"
            Grid.Row="1"
            Width="24"
            Height="24"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            IsActive="False"
            Visibility="Collapsed" />

        <TextBlock
            x:Name="StatusText"
            Grid.Row="1"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Style="{ThemeResource BodyTextBlockStyle}"
            TextWrapping="Wrap"
            Visibility="Collapsed" />

        <HyperlinkButton
            x:Name="ReleaseLink"
            Grid.Row="2"
            Padding="0,4"
            Content="View on GitHub" />
    </Grid>
</UserControl>
