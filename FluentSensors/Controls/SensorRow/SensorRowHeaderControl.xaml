<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="FluentSensors.Controls.SensorRow.SensorRowHeaderControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    HorizontalAlignment="Stretch"
    HorizontalContentAlignment="Stretch"
    IsTabStop="False"
    mc:Ignorable="d">

    <Grid HorizontalAlignment="Stretch">

        <!--
            (the same columns as the CardContent grid in SensorRowControl, which has to change together with this one;
            the value columns carry its MaxWidth as well, workaround: star columns fill greedily)
        -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition
                x:Name="NameColumn"
                Width="5*"
                MinWidth="70" />
            <ColumnDefinition
                x:Name="ThresholdColumn"
                Width="36"
                MinWidth="36" />
            <ColumnDefinition
                x:Name="CurrentColumn"
                Width="2*"
                MinWidth="60"
                MaxWidth="10000" />
            <ColumnDefinition
                x:Name="MinimumColumn"
                Width="2*"
                MinWidth="60"
                MaxWidth="10000" />
            <ColumnDefinition
                x:Name="MaximumColumn"
                Width="2*"
                MinWidth="60"
                MaxWidth="10000" />
            <ColumnDefinition
                x:Name="AverageColumn"
                Width="2*"
                MinWidth="60"
                MaxWidth="10000" />
            <ColumnDefinition
                x:Name="UnitColumn"
                Width="0"
                MinWidth="0" />
        </Grid.ColumnDefinitions>

        <!--  (the left margin lines the label up with the sensor name, past the checkbox of the row)  -->
        <TextBlock
            Grid.Column="0"
            Margin="38,0,0,0"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Style="{ThemeResource CaptionTextBlockStyle}"
            Text="Sensor"
            TextTrimming="CharacterEllipsis"
            TextWrapping="NoWrap" />
        <TextBlock
            x:Name="ThresholdLabel"
            Grid.Column="1"
            HorizontalAlignment="Center"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Style="{ThemeResource CaptionTextBlockStyle}"
            Text="T"
            TextTrimming="CharacterEllipsis"
            TextWrapping="NoWrap" />
        <!--<FontIcon
            Grid.Column="1"
            HorizontalAlignment="Center"
            FontSize="12"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Glyph="&#xE7C1;" />-->
        <TextBlock
            x:Name="CurrentLabel"
            Grid.Column="2"
            HorizontalAlignment="Right"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Style="{ThemeResource CaptionTextBlockStyle}"
            Text="Current"
            TextTrimming="CharacterEllipsis"
            TextWrapping="NoWrap" />
        <TextBlock
            x:Name="MinimumLabel"
            Grid.Column="3"
            HorizontalAlignment="Right"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Style="{ThemeResource CaptionTextBlockStyle}"
            Text="Min"
            TextTrimming="CharacterEllipsis"
            TextWrapping="NoWrap" />
        <TextBlock
            x:Name="MaximumLabel"
            Grid.Column="4"
            HorizontalAlignment="Right"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Style="{ThemeResource CaptionTextBlockStyle}"
            Text="Max"
            TextTrimming="CharacterEllipsis"
            TextWrapping="NoWrap" />
        <TextBlock
            x:Name="AverageLabel"
            Grid.Column="5"
            HorizontalAlignment="Right"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Style="{ThemeResource CaptionTextBlockStyle}"
            Text="Avg"
            TextTrimming="CharacterEllipsis"
            TextWrapping="NoWrap" />
        <TextBlock
            x:Name="UnitLabel"
            Grid.Column="6"
            HorizontalAlignment="Right"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Style="{ThemeResource CaptionTextBlockStyle}"
            Text="Unit"
            TextTrimming="CharacterEllipsis"
            TextWrapping="NoWrap"
            Visibility="Collapsed" />
    </Grid>
</UserControl>
