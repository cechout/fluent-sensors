<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="FluentHwInfo.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FluentHwInfo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="FluentHwInfo">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <NavigationView x:Name="nvSample"
                        PaneDisplayMode="Auto"
                        SelectionChanged="nvSample_SelectionChanged">

            <NavigationView.MenuItems>
                <NavigationViewItem Icon="Home"
                                    Content="CPU"
                                    Tag="CPU" />
                <NavigationViewItem Icon="DockRight"
                                    Content="GPU"
                                    Tag="GPU" />
            </NavigationView.MenuItems>

            <NavigationView.FooterMenuItems>
                <!--settings tab is automatically included-->
            </NavigationView.FooterMenuItems>

            <Frame x:Name="contentFrame" />

        </NavigationView>
    </Grid>
</Window>
